diff --git a/infoic.xml b/infoic.xml
index 4a39409..36a8dc3 100644
--- a/infoic.xml
+++ b/infoic.xml
@@ -483610,6 +483610,55 @@
       />
     </custom
     >
+    <custom
+        name="EPSON"
+      >
+      <ic
+          name="LQ500_4C_PIN22_HIGH@DIP28"
+          type="1"
+          protocol_id="0x80000001"
+          variant="0x0d"
+          read_buffer_size="0x10"
+          write_buffer_size="0x00"
+          code_memory_size="0x10000"
+          data_memory_size="0x00"
+          data_memory2_size="0x00"
+          page_size="0x0000"
+          pages_per_block="0x0000"
+          chip_id="0x00000000"
+          voltages="0x0900"
+          pulse_delay="0x0000"
+          flags="0x00000000"
+          chip_info="0x0000"
+          pin_map="0x0017"
+          blank_value="0x00"
+          package_details="0x1c000000"
+          config="NULL"
+      />
+      <ic
+          name="LQ500_4C_A16_PIN22@DIP28"
+          type="1"
+          protocol_id="0x80000001"
+          variant="0x0e"
+          read_buffer_size="0x10"
+          write_buffer_size="0x00"
+          code_memory_size="0x20000"
+          data_memory_size="0x00"
+          data_memory2_size="0x00"
+          page_size="0x0000"
+          pages_per_block="0x0000"
+          chip_id="0x00000000"
+          voltages="0x0900"
+          pulse_delay="0x0000"
+          flags="0x00000000"
+          chip_info="0x0000"
+          pin_map="0x0017"
+          blank_value="0x00"
+          package_details="0x1c000000"
+          config="NULL"
+      />
+    </custom
+    >
   </database
   >
   <database
diff --git a/src/prom.c b/src/prom.c
index 7118eea..7e10d13 100644
--- a/src/prom.c
+++ b/src/prom.c
@@ -162,6 +162,27 @@ static prom_t prom_table[] = {
 		  (uint8_t[]){ 8, 7, 6, 5, 4, 3, 2, 1, 23, 22, 19, 18, 21, 0 },
 	  .ce_lo_pins = (uint8_t[]){ 20, 0 },
 	  .compare_mask = 0xff },
+
+	/* Type 13; 65536x8; DIP28 27C512-style mask ROM, pin 22 held high */
+	{ .gnd_pins = (uint8_t[]){ 14, 0 },
+	  .vcc_pins = (uint8_t[]){ 28, 0 },
+	  .data_bus_pins = (uint8_t[]){ 11, 12, 13, 15, 16, 17, 18, 19, 0 },
+	  .addr_bus_pins =
+		  (uint8_t[]){ 10, 9, 8, 7, 6, 5, 4, 3,
+			       25, 24, 21, 23, 2, 26, 27, 1, 0 },
+	  .ce_lo_pins = (uint8_t[]){ 20, 0 },
+	  .ce_hi_pins = (uint8_t[]){ 22, 0 },
+	  .compare_mask = 0xff },
+
+	/* Type 14; 131072x8; DIP28 27C011-style mask ROM, pin 22 as A16 */
+	{ .gnd_pins = (uint8_t[]){ 14, 0 },
+	  .vcc_pins = (uint8_t[]){ 28, 0 },
+	  .data_bus_pins = (uint8_t[]){ 11, 12, 13, 15, 16, 17, 18, 19, 0 },
+	  .addr_bus_pins =
+		  (uint8_t[]){ 10, 9, 8, 7, 6, 5, 4, 3,
+			       25, 24, 21, 23, 2, 26, 27, 1, 22, 0 },
+	  .ce_lo_pins = (uint8_t[]){ 20, 0 },
+	  .compare_mask = 0xff },
 };
 
 static mask_prom_t mask_prom_table[] = {
